namespace DataStructures;

/// <summary>
/// How a <see cref="FigureDrawData"/>'s assigned graphs should be labeled with letters
/// (in row-major order over the cells that have a graph), or not labeled at all.
/// </summary>
public enum FigureLabelStyle
{
    None,
    Uppercase,
    Lowercase
}
